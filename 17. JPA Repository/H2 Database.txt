H2 DB -> Java기반의 경량화된 database로 파일로 저장해 
실제 DB처럼 데이터를 유지할 수도 있고 메모리DB로 동작하여 
실제 instance가 동작할때만 유지하기도 한다.


H2 Database는 실행을 하고 종료를 할 때마다 메모리에 저장된 것이 사라지게 된다.

Test를 하는 경우 초기 데이터들이 필요할 텐데 그러한 경우
resources폴더 안에 sql파일을 생성하여 초기 데이터를 불러오게 할 수 있다.